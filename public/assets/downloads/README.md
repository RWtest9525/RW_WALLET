# APK Storage Folder

Place your `app.apk` file inside this directory (`public/files/app.apk`).

When users visit `GET /download?ref=YOUR_CODE`, the server will serve `public/files/app.apk` automatically!
